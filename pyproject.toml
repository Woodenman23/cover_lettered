[project]
name = "cover-lettered"
version = "2.0.0"
description = "Cover Letter automation tool."
authors = [
    {name = "Joe Foster",email = "joe.foster@protonmail.com"}
]
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "flask (>=3.1.1,<4.0.0)"
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
